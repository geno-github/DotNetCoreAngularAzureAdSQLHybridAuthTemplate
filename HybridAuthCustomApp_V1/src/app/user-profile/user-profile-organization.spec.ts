import { UserProfileOrganization } from './user-profile-organization';

describe('UserProfileOrganization', () => {
  it('should create an instance', () => {
    expect(new UserProfileOrganization()).toBeTruthy();
  });
});

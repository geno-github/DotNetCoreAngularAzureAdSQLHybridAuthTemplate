import { UserProfileOrganizations } from './user-profile-organizations';

describe('UserProfileOrganizations', () => {
  it('should create an instance', () => {
    expect(new UserProfileOrganizations()).toBeTruthy();
  });
});
